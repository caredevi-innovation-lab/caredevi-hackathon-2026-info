# Core HTTP + retry
requests>=2.31.0
tenacity>=8.2.3

# FHIR R4 schema validation & resource construction
fhir.resources>=8.0.0

# Date/time utils
python-dateutil>=2.8.2

# Optional: pretty CLI output for the pipeline (not required for app integration)
rich>=13.0.0
